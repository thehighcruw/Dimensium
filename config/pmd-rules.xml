<?xml version="1.0"?>
<ruleset name="Dimensium PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>Code quality rules for Dimensium</description>

    <!-- Method length -->
    <rule ref="category/java/design.xml/NcssCount">
        <properties>
            <property name="methodReportLevel" value="500"/>
            <property name="classReportLevel" value="2000"/>
        </properties>
    </rule>

    <!-- Cyclomatic complexity -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="methodReportLevel" value="250"/>
            <property name="classReportLevel" value="500"/>
        </properties>
    </rule>

    <!-- Cognitive complexity -->
    <rule ref="category/java/design.xml/CognitiveComplexity">
        <properties>
            <property name="reportLevel" value="300"/>
        </properties>
    </rule>

    <!-- Fully-qualified names in code (should use imports) — priority 1: build error -->
    <rule ref="category/java/codestyle.xml/UnnecessaryFullyQualifiedName">
        <priority>1</priority>
    </rule>

    <!-- Duplicate imports — priority 1: build error -->
    <rule ref="category/java/codestyle.xml/DuplicateImports">
        <priority>1</priority>
    </rule>

    <!-- Unused imports — priority 1: build error -->
    <rule ref="category/java/bestpractices.xml/UnusedImports">
        <priority>1</priority>
    </rule>

</ruleset>
